using namespace std;
#include <iostream>


int is_prime(int i)
{
	if(i <= 1)
		return 0;
	if(i % 2 == 0)
		return (0);
	while (i < (num < 2))
		{
			if (n % i == 0)
				return 0;
			i += 2
		}
	return 1;
}

int main (int a, char **b)
{
	
	int n = atoi(b[1])
	while (n > 0)
		if (is_prime(n--))
		{
			sum 
		}
	while (n)
}